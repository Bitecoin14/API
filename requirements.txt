opencv-contrib-python>=4.8,<4.11
mediapipe>=0.10.9,<=0.10.14   # 0.10.15+ removed the legacy solutions API used here
numpy>=1.24,<2.0
scikit-learn>=1.3
joblib>=1.3
Pillow>=9.5.0                 # EXIF-aware photo loading for enrollment

# Face recognition (optional — falls back to LBPH without these)
insightface>=0.7.3
onnxruntime>=1.17.0,<2.0      # CPU inference; replace with onnxruntime-gpu for NVIDIA GPU
ultralytics>=8.1.0            # YOLOv8-face cross-check detector
